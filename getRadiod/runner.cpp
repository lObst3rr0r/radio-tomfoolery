#include <Windows.h>

int main()
{
    // Run the first executable
    system("start windowfuckery.exe");

    system("start getRadiod.exe");

    // Run the second executable
    system("exit");
    return 0;
}